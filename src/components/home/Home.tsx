import React from 'react';

export default function Home () {
    return (
        <p>Examples from the book "Webgl Programming Guide - ..." -
            Click on one of them (in the left nav bar) to see them.</p>
    );
}
